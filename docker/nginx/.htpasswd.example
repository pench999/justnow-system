# Copy this file to docker/nginx/.htpasswd and add users with:
#   docker run --rm httpd:2.4-alpine htpasswd -nbB USERNAME PASSWORD
#
# Example output line format:
# username:$2y$05$.....................................................
